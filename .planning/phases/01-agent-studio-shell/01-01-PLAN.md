---
phase: 1
phase_name: Agent Studio Shell
plan: 01
title: App Shell And Agent Configuration Dashboard
type: implementation
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/page.tsx
  - src/components/AgentForm.tsx
  - src/components/AgentDashboard.tsx
  - src/lib/agentStore.ts
  - src/lib/types.ts
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05
  - DASH-01
---

# Plan 01: App Shell And Agent Configuration Dashboard

<objective>
Build the first working AgentBid Studio slice: a polished single-user dashboard where a user can create a bidding agent, configure campaign and safety fields, and see the new agent listed with paused status for the current browser session.
</objective>

<context>
Phase 1 is the walking skeleton for the hackathon demo. There is no existing application scaffold in the repo, so this plan must create the frontend foundation and the initial in-memory agent workflow. Prioritize a credible buy-side control-plane UI over backend depth.
</context>

<requirements_addressed>
- AGENT-01: User can create a bidding agent with a campaign/agent name and brand.
- AGENT-02: User can set a campaign goal.
- AGENT-03: User can set a daily budget cap and max CPC.
- AGENT-04: User can choose manual, assisted, or autonomous mode.
- AGENT-05: User can specify target intents and blocked categories.
- DASH-01: Dashboard lists configured agents with name, brand, and status.
</requirements_addressed>

<truths>
- The first submitted agent must appear with status `paused`.
- Agent data only needs to persist across navigation within the running session; in-memory React state is acceptable.
- The UI should look like a credible buy-side product dashboard, not a toy form demo.
- No real ad exchange, auth, database, payments, or external API integration is required in Phase 1.
- The implementation may choose the fastest viable frontend stack, but should keep later phases easy: live simulation, analytics, safety controls, and approval queue.
</truths>

<tasks>

## Task 1: Scaffold The Frontend App

type: setup
files:
- `package.json`
- `src/app/page.tsx` or equivalent app entry file
- `src/styles` or framework default style files

action:
- Initialize a lightweight React/Next.js app in the existing repo without disturbing `.planning/` artifacts.
- Add TypeScript if available in the scaffold path.
- Add a minimal styling setup that supports a polished dashboard quickly. Tailwind is acceptable if the scaffold makes it cheap; otherwise use plain CSS modules/global CSS.
- Ensure the default app starts locally with the standard dev command.

verify:
- `npm install` completes.
- `npm run dev` starts the app.
- Opening the app renders AgentBid Studio, not a default starter page.

acceptance_criteria:
- A browser can load the app at the local dev URL.
- The repository has a clear frontend entry point for later phases.

## Task 2: Define Agent Configuration Types And Session Store

type: implementation
files:
- `src/lib/types.ts`
- `src/lib/agentStore.ts` or state colocated in the main app if the app is intentionally tiny

action:
- Define an `AgentConfig` model with:
  - `id`
  - `agentName`
  - `brandName`
  - `goal`
  - `dailyBudget`
  - `maxCpc`
  - `autonomyMode`
  - `targetIntents`
  - `blockedCategories`
  - `status`
  - `createdAt`
- Use status values compatible with later phases: `active`, `paused`, `needs_review`.
- Implement session-scoped state for a list of agents.
- Seed one believable demo agent only if it helps the empty dashboard look credible; newly created agents must still be distinguishable.

verify:
- Type checking catches invalid status or autonomy values.
- New agents are added with `status: "paused"`.

acceptance_criteria:
- State survives normal in-app navigation during the session.
- No backend or persistent database is introduced.

## Task 3: Build The Create Agent Form

type: implementation
files:
- `src/components/AgentForm.tsx`
- `src/app/page.tsx`

action:
- Build a form to capture:
  - agent/campaign name
  - brand
  - campaign goal
  - daily budget cap
  - max CPC
  - autonomy mode: manual, assisted, autonomous
  - target intents
  - blocked categories
- Use controls suited to the fields: segmented/radio controls for autonomy, numeric inputs for budget and CPC, compact token-style text entry or comma-separated fields for intents/categories.
- Validate required text fields and positive numeric values.
- On submit, create the agent, reset the form, and keep the user on the dashboard.

verify:
- Submit is blocked or clearly invalid when required fields are missing.
- Budget and CPC cannot be negative.
- Submitting a valid form creates exactly one new agent.

acceptance_criteria:
- The user can launch/use the create-agent flow without reading instructions.
- Every Phase 1 agent configuration field is captured.

## Task 4: Build The Agent Dashboard List

type: implementation
files:
- `src/components/AgentDashboard.tsx`
- `src/app/page.tsx`

action:
- Render a dashboard list/table of configured agents.
- For each agent, show at minimum:
  - agent/campaign name
  - brand
  - goal summary
  - daily budget
  - max CPC
  - autonomy mode
  - status badge
  - target intents and blocked categories
- Ensure newly created agents appear immediately with status `paused`.
- Include empty state copy only if there are no configured agents.

verify:
- Create an agent and confirm it appears in the dashboard without reload.
- Create a second agent and confirm both are visible.
- Confirm the status label is `paused` for new agents.

acceptance_criteria:
- DASH-01 is visibly satisfied.
- The dashboard structure leaves clear room for Phase 2 live spend/status updates.

## Task 5: Apply Product-Grade UI Polish

type: implementation
files:
- `src/app/page.tsx`
- `src/components/*`
- relevant style files

action:
- Shape the first viewport as the actual dashboard experience, not a marketing landing page.
- Use a dense but readable SaaS/control-plane layout: header, form/config panel, agent list, and compact KPI/status placeholders where useful.
- Avoid overdecorated hero sections or toy styling.
- Make the UI responsive enough for laptop demo use and passable on mobile.
- Use button labels and status text that fit their containers.

verify:
- Inspect the app in desktop viewport.
- Inspect a narrow viewport.
- Confirm no text overlaps or form controls overflow.

acceptance_criteria:
- The dashboard reads as a credible buy-side agent control product.
- Phase 2 can add live bidding feed and metrics without redesigning the page from scratch.

## Task 6: Verification And Handoff

type: verification
files:
- `README.md`
- generated app files

action:
- Run available checks: typecheck, lint, and/or build depending on scaffold support.
- Start the dev server and verify the Phase 1 flow manually:
  1. open app
  2. submit a travel-brand agent
  3. confirm dashboard list shows name, brand, and paused status
  4. navigate within the app if routing exists and confirm session state remains
- Add a short README note only if the scaffold command changes how to run the project.

verify:
- `npm run build` or the strongest available equivalent passes.
- Manual browser check confirms the success criteria.

acceptance_criteria:
- All Phase 1 success criteria pass.
- Any skipped verification is explicitly documented in the phase summary.

</tasks>

<verification>
Run the strongest available local checks after implementation:

```powershell
npm install
npm run build
```

If the scaffold includes lint/typecheck scripts, run those too. Then start the app and verify manually in a browser:

- the dashboard opens
- create-agent form accepts all required fields
- a valid submit adds an agent to the dashboard
- the new agent status is `paused`
- the created agent remains visible across in-app navigation during the session
</verification>

<success_criteria>
- User can open the app and see a dashboard listing configured agents with name, brand, and status.
- User can create an agent with name, brand, goal, daily budget cap, max CPC, autonomy mode, target intents, and blocked categories.
- Newly created agents appear immediately with status `paused`.
- Agent configuration persists within the current browser session.
- The UI is polished enough for a hackathon judging demo and structured for Phase 2 additions.
</success_criteria>
